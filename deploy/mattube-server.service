[Unit]
Description=mattube server (Drive poller + job processor)
After=network.target

[Service]
Type=simple
User=mattube
WorkingDirectory=/opt/mattube
ExecStart=/usr/bin/mattube-server
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
